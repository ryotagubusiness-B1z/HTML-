# CLAUDE.md

このリポジトリは CSV と画像で動く、ローカル動作のゲームブックです。
構成・動かし方・CSV 仕様は **README.md** を参照してください。

## Codex への委譲（クレジット節約）

クレジット節約のため、**Codex CLI が得意な作業や Claude Code と同等に行える作業は
Codex に委譲**します。Codex はセッション開始フック
(`.claude/hooks/session-start.sh`) が毎セッション自動でインストール・認証します。

### 前提
- 自動セットアップはリモート(web)セッションでのみ動作します（`CLAUDE_CODE_REMOTE=true`）。
- 動作には環境変数 `OPENAI_API_KEY` が必要です（Claude Code web の環境設定で追加）。
  未設定なら委譲せず、Claude が直接対応してください。
- 利用可否の確認: `codex login status`（"Logged in" なら委譲可能）。

### Codex に委譲するタスク（原則こちらは委譲する）
- 大量・定型のコード生成（ボイラープレート、設定ファイル、データ生成）
- 機械的なリファクタや複数ファイルにまたがる一括置換
- 既存コードに対するテストの自動生成
- 仕様が明確で判断の余地が少ない実装
- コードの説明・調査・要約

### Claude が直接行うタスク（委譲しない）
- アーキテクチャ／設計判断、トレードオフ評価
- 深い推論や慎重な判断が必要なもの
- Codex 出力のレビュー・統合・最終確認
- セキュリティ／安全性に関わる判断
- ユーザーとの対話・要件定義

### 呼び出し方（Bash 経由）
```bash
# 解析・説明・調査（書き込み不要。既定はサンドボックス読み取り）
codex exec "js/script.js のページ遷移ロジックを要約して"

# ファイル生成・編集を伴う作業（ワークスペース書き込みを許可）
codex exec -s workspace-write "data/data.csv の choice*_to が存在しないページを指していないか検査して報告"

# 最終メッセージをファイルに保存 / 機械処理用に JSON 出力
codex exec -o /tmp/out.txt "..."
codex exec --json "..."
```
- Codex はカレントディレクトリを作業ルートにします（変更したい場合は `-C <dir>`）。
- Git リポジトリ外で動かす場合のみ `--skip-git-repo-check` を付与します。

### ルール
1. **Codex の出力は必ず Claude がレビュー**してからコミットする（鵜呑みにしない）。
2. `codex` が未インストール／未認証なら、委譲せず Claude が直接対応する。
3. 破壊的・不可逆な操作や外部公開を伴う作業は委譲しない。
